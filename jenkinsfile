node{
def mavenHome = tool name: "maven_3.8.4"
properties([buildDiscarder(logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '5', daysToKeepStr: '5', numToKeepStr: '5')), [$class: 'JobLocalConfiguration', changeReasonComment: ''], pipelineTriggers([pollSCM('* * * * *')])])
stage('CheckoutCode')
{
git branch: 'development', credentialsId: '8351abdb-3c6a-4945-8f8d-a5898d1b8b31', url: 'https://github.com/MAHESHASM/maven-web-application.git'
}

stage('Build')
{
sh "${mavenHome}/bin/mvn clean package"
}
/*
stage('executesonarqubereport')
{
sh "${mavenHome}/bin/mvn sonar:sonar"
}

stage('artifacatsintoto nexus')
{
sh "${mavenHome}/bin/mvn deploy"
}

stage('Deployapllicationinto tomcat')
{
sshagent(['38f7b055-3624-40d5-8a15-d5a3a241c967']) {
    // some block
sh "scp -o  StrictHostKeyChecking=no target/maven-web-application.war ec2-user@172.31.32.30:/opt/apache-tomcat-9.0.63/webapps"
}
}
*/

}

